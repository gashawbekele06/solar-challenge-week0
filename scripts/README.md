# scripts
Optional helper scripts for fetching/preprocessing data.

- Output CSVs into `../data/` (git-ignored).
- Example (pseudo): `python fetch_solar.py --out ../data/ghi_jan2024.csv`

Ensure the app reads `./data/*.csv`.
